# Cogwork — Build Log

Append-only, terse. One line per meaningful event. Times are local (America/Los_Angeles).

Format: `YYYY-MM-DD HH:MM  StageX  done|BLOCKED|note: <message>  [commit shortsha]`

---

2026-06-13 14:44  Setup   env verified: node 25.9, pnpm 10.13, docker(colima) up, git+gh authed (sahielbose)
2026-06-13 14:44  Setup   note: host postgres on :5432 + docker pg on :5433/:5434 — Cogwork DB runs on :5435 (docker run; compose plugin is a broken symlink to absent Docker Desktop)
2026-06-13 14:44  Setup   note: ANTHROPIC_API_KEY not present in env/.env — left blank; generation code is real (AI SDK + Anthropic) but live generation needs the human to add the key. Tests mock the LLM (per spec §23.3).
2026-06-13 14:44  Setup   .env written from .env.example; ENCRYPTION_KEY + AUTH_SECRET generated; COGWORK_CONNECTORS=fixture
2026-06-13 14:44  StageB  note: DB helper/integration tests use embedded pglite (real Postgres in WASM) so the green gate never depends on Docker; db:push/db:migrate target real Postgres via DATABASE_URL.
2026-06-13 15:05  Stage0  done: monorepo bootstrap green (typecheck+lint+test+build all pass on empty repo); 8 logical commits.
2026-06-13 15:06  Stage0  BLOCKED: `git push` of .github/workflows/* rejected — gh OAuth token lacks `workflow` scope. Workaround: CI defs staged in ci/ (reviewable, ready); human runs `gh auth refresh -s workflow` then moves them (see ci/README.md). Does not affect local green gate.
2026-06-13 15:18  StageA  done: packages/spec — schema (WorkflowSpecSchema), binding resolver + condition grammar, semantic validator (5 checks of §4.4) + approval inference. 35 tests (resolver + validator table cases). Green gate ✓.
2026-06-13 15:30  StageB  done: packages/db — Drizzle schema (all 10 v1 tables) + generated migrations, postgres.js client, migrate runner, typed query helpers (prefs/users/workflows/runs/steps/approvals/audit). db:push applies cleanly to real PG :5435; 5 helper tests vs pglite. Green gate ✓.
2026-06-13 15:45  StageC  note: gmail/gcal/slack are fixture-only (no live impls) — live API calls are Phase 3 ("no live connectors"). ai.generate has a real live path (AI SDK + Anthropic) + deterministic fixture. Tokens enc stored as base64 text.
2026-06-13 15:45  StageC  done: packages/connectors — SDK (types/registry/getToolCatalog), AES-256-GCM crypto, oauth2 helper (code only), fixture/live run-mode, ai.generate (live+fixture) + gmail/gcal/slack actions + fixtures. 26 tests. Green gate ✓.
2026-06-13 16:00  StageD  done: packages/engine — deterministic interpreter (bindings, run_steps, retries w/backoff, approval pause→awaiting_approval, resume-safe context, idempotent no-double-invoke), redaction, idempotency keys, cost/token tracking. 12 tests incl. e2e run + pause + resume. Green gate ✓.
2026-06-13 16:15  StageE  done: packages/builder — real compile (generateObject + catalog injection + validate-and-repair loop + generateText fallback; LLM call injectable for deterministic CI), deterministic explain + codegen, prompts/DSL reference. packages/evals — §9.2 morning-briefing golden case + 2 more (mocked + live modes). 24 tests. All 3 MUST-BE-REAL pieces (validator/engine/generation) done. Green gate ✓.
2026-06-13 16:55  StageF  note: monorepo .env lives at repo root; Next loads .env from apps/web — fixed by loading ../../.env in next.config.mjs (dotenv).
2026-06-13 16:55  StageF  note: Auth.js = NextAuth v5; needed declaration:false in apps/web tsconfig (TS2742 pnpm portability). Login is credential-free (email-only dev provider) + JWT; Google OAuth is go-live.
2026-06-13 16:55  StageF  VERIFIED hero run-loop e2e via live dev server: sign in → create workflow (canned spec) → run → fetch+brief succeed, side-effect step pauses (awaiting_approval) → approve → resume → succeeded. PII redacted in persisted trace (y***@yourco.com). Fixture connectors, no creds.
2026-06-13 16:55  StageF  BLOCKED (partial): live GENERATION from a prompt needs ANTHROPIC_API_KEY (not present). Generation code is REAL (AI SDK + Anthropic) and the §9.2 mocked test passes; the compile route returns an honest "set ANTHROPIC_API_KEY" 503 until the human adds the key. Run→trace→approve loop fully verified without it.
2026-06-13 17:25  StageG  done: packages/scheduler (pg-boss) — sync logic + per-workflow cron + RUN_QUEUE worker + runWorkflowJob; OAuth auto-refresh (ensureFreshAccessToken) + needs_reauth db helpers; webhook trigger route (/api/hooks/:path → trigger.payload); engine reliability (retry/backoff, partial-failure resume, durable approval resume, cost tracking) hardened + tested. Fixed real bug: failed runs weren't setting status=failed.
2026-06-13 17:25  StageG  VERIFIED pg-boss fires against real Postgres (one-off script): cron registered + RUN_QUEUE job dequeued and executed to success. Green-gate scheduler tests use a fake scheduler (no running queue needed).
2026-06-13 17:55  StageH  done (pt1): connectors notion/github/postgres(guarded execute)/apify/http + fixtures (9 connectors total); builder NL edit→diff (re-validated) + chat editing + /api/builder/edit; memory (prefs API + Memory page, fed into builder prompt); templates gallery + clone-to-draft + 5 valid starter templates; workflow versions + rollback + Workflow detail (Overview/Runs/Versions/Settings); TS export; Settings (profile/API-keys UI/appearance/audit). Green gate ✓.
2026-06-13 18:20  StageH  done (pt2): broadened eval golden cases (postgres/github/webhook). Decoupled web typecheck from .next/types (exclude .next) so the green gate is order-independent.
2026-06-13 18:30  StageI  done: marketing site — pill nav + footer + (marketing) layout; Home (interactive hero w/ tabs + typed prompt + flow-canvas playback, how-it-works, community proof [honest OSS signals, no investor logos], why + integration orbit, conversational builder, FAQ accordion, final CTA); Open Source section (repo card + real terminal quickstart); Pricing, Integrations directory (live registry), Demos/Templates, Docs shell, Legal (terms/privacy), llms.txt. Green gate ✓.
2026-06-13 16:55  StageF  done: apps/web Phase-0 UI — tokens/theme/fonts, UI kit, auth+login, app shell, Connections (fixture banner), Builder three-pane (chat + xyflow canvas + spec), Dashboard, Run detail (trace/redacted I/O/approve), Approvals; routes: compile, connectors, workflows(+run/activate/pause), runs, approvals. Green gate ✓.
